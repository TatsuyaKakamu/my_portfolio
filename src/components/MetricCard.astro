---
const { icon: Icon, label, value, unit, note } = Astro.props;
---

<article class="metric-card">
  <Icon size={28} aria-hidden="true" />
  <span>{label}</span>
  <strong>
    {value}
    <small>{unit}</small>
  </strong>
  <em>{note}</em>
</article>
